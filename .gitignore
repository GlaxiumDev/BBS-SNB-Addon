# Project exclude paths
/.gradle/
/build/
/bin/
/libs/
/run/
/.idea/
/.claude/
/.freebuff/

# Vendored reference checkouts (bbs-fs, bbs-mod, bbs-cml) used only for
# cross-referencing real BBS fork source/bytecode during development -- not
# a build dependency, never published.
/reference-sources/
