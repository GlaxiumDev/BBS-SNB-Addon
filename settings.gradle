pluginManagement {
	repositories {
		// Local-only plugin resolution. Always build with --offline.
		maven {
			name = 'OfflineLoom'
			url = uri("${rootDir}/libs/offline-maven")
		}
	}
}

rootProject.name = "BBS_SNB_Addon"
